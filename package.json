{
  "name": "sql-slonik-express-exercise",
  "version": "1.0.0",
  "description": "1. Comprueba que tienes corriendo los contenedores de `postgres` y `adminer` a través de `docker-compose`",
  "main": "constants.js",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js",
    "docker:up":"docker-compose up",
    "db:up":"node scripts/creation.js && node scripts/insertion.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "slonik": "^28.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
